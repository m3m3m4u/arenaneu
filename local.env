#############################
# Lokale Entwicklungs-Umgebung (.env NICHT einchecken mit realen Secrets!)
# Datei kopieren/umbenennen zu .env.local und Werte füllen.
#############################

# --- Basis NextAuth / App ---
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme-dev-secret

# MongoDB
MONGODB_URI=mongodb://localhost:27017/arenaneu

# --- Allgemeines WebDAV für Medien (Bilder, Videos etc.) ---
WEBDAV_BASEURL=https://your-media-box.example.com/dav/arenaneu
WEBDAV_USERNAME=media-user
WEBDAV_PASSWORD=media-pass
WEBDAV_PUBLIC_BASEURL=https://cdn-media.example.com/arenaneu

# --- Shop / Materialien separater WebDAV (optional) ---
# Falls gesetzt, überschreibt dieser Bereich die oben stehenden WEBDAV_* nur für Produkt-/Material-Dateien.
SHOP_WEBDAV_BASEURL=https://your-shop-box.example.com/dav/arenashop
SHOP_WEBDAV_USERNAME=shop-user
SHOP_WEBDAV_PASSWORD=shop-pass
SHOP_WEBDAV_PUBLIC_BASEURL=https://cdn-shop.example.com/files

# Prefix-Ordner auf dem (Shop-)WebDAV für Produktdateien
WEBDAV_SHOP_PREFIX=shop

# --- Optional S3 Fallback / Alternative ---
S3_ENDPOINT=localhost:9000
S3_REGION=eu-central-1
S3_ACCESS_KEY_ID=minio
S3_SECRET_ACCESS_KEY=minio123
S3_BUCKET=arenaneu
S3_PUBLIC_BASEURL=

# --- Sonstiges (Beispiele) ---
# RATE_LIMIT_WINDOW=60000
# RATE_LIMIT_MAX=100

#############################
# Hinweise:
# - Niemals echte Zugangsdaten committen.
# - Für Produktion separate .env Variablen (z.B. in Vercel Dashboard) setzen.
# - SHOP_WEBDAV_* weglassen => allgemeine WEBDAV_* werden für Shop verwendet.
#############################
